auto-install-peers=true
strict-peer-dependencies=false
# Pin the exact registry so the lockfile is reproducible across machines.
registry=https://registry.npmjs.org/
# Version-resolution strategy only — reproducibility is enforced by
# `pnpm install --frozen-lockfile` in CI, not by this setting.
resolution-mode=highest
