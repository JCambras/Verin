# Review gallery - every bucket-B artifact, indexed (CONSTITUTION.md, the review budget)

One row per binary or captured-evidence artifact: path, SHA-256, bytes, authorship, purpose, inspection.

| Path | SHA-256 | Bytes | Authorship / capture | Purpose | Inspect |
|---|---|---:|---|---|---|
| `src/app/fonts/GeistVF.woff2` | `46525b05bbf4527d263f8624ca7024c7d0dce3c64430267d30c31c6dd3821e4d` | 58512 | Vercel's Geist variable font, extracted from npm `geist@1.5.1` `package/dist/fonts/geist-sans/Geist-Variable.woff2` (`npm pack geist@1.5.1`) | self-hosted `next/font/local` typeface; `next/font/google` is forbidden (prompt 2 5A) | `shasum -a 256` and load any page |
| `docs/evidence/pr2a-signin.png` | `e90592e97527394bfebc528eb0dcd010d858c91cdebc51019214f15940d0e574` | 16248 | Playwright screenshot at 1280x800 from `src/e2e/app.e2e.ts` (URL and loaded-state marker asserted first) | section 9: the sign-in surface as shipped | open the file; re-capture with `pnpm e2e` |
| `docs/evidence/pr2a-shell.png` | `1fd9b21132cf0d0a8cea1ea1c15073c300002d3272147d52f80733f52c172454` | 33853 | Playwright screenshot at 1280x800 from `src/e2e/app.e2e.ts`, signed in as the seeded advisor | section 9: the honest empty shell PR-2a lands on (the split's user-visible result) | open the file; re-capture with `pnpm e2e` |
| `docs/evidence/pr2a-transcripts.tar` | `91bf94158d12bc862f2453341158436ef8abf7eec384ed5a7382e29e62915ad6` | 73216 | `tar -cf` over the PR-2a mutation and proof transcripts (one captured-evidence bundle per PR, prompt 2 section 7; plain tar so the release-artifact classifier never mistakes evidence for a release subject) | every raw transcript behind `docs/proof-log.md`'s PR-2a entries: M-B, M-D, M-E, M-F, M-G, the collector-sharpening proofs, pin currency, migration preflight, the restored green run, and the F2/F5 amendments from the independent pass; `m-e-7-8.txt` and `m-e-9-runtime.txt` also keep superseded first attempts as historical raw evidence - `m-e-8-fixed.txt` and `m-e-9-rerun.txt` are the operative records | `tar -tf`, then `tar -xf` and read |
| `docs/evidence/verin-0.2.0.tgz` | `6e246a048930507266f8a6c2b29a1fef1dff5a1f8ba1654c07dc154aeab60b0e` | 161097 | `corepack pnpm pack` at the PR head's source (a release artifact never contains its own checksum row, so its bytes predate this line) | the first real release subject: E15 leaves its honest-empty state against it, with `docs/evidence/verin-0.2.0.cdx.json` (bucket G, 291 components) equal to the frozen lockfile in both directions | `tar -tzf`; `corepack pnpm db sbom` regenerates the SBOM byte-identically |
| `docs/evidence/pr2a2-households.png` | `f1a020215ccad81fa5b751cb44e87559940c1782343ba4cdd92d59ee4207399a` | 30049 | Playwright screenshot at 1280x800 from `src/e2e/app.e2e.ts`, Firm A's advisor on the register | section 9: the household register (PR-2a' - the split's deferred screenshot), every seeded row wearing its demonstration chip | open the file; re-capture with `pnpm e2e` |
| `docs/evidence/pr2a2-transcripts.tar` | `de1b444ee0c183d45abaf485aaf8e16cddbf1397f8ff684039be87c58b8cd401` | 14848 | `tar -cf` over the PR-2a' proof transcripts (one captured-evidence bundle per PR) | the household-register smuggling refusal with its exact mutated bytes, the unregistered-step refusal, the RLS-removed counter-run failing and the restored pass, and the restored green run | `tar -tf`, then `tar -xf` and read |
| `docs/evidence/pr2b-workspace.png` | `f9a2c6f9bd50efbc6681aace5c02e3a95f834c42ceec75ec932c4ebcf45d28af` | 85072 | Playwright screenshot at 1280x800 from `src/e2e/app.e2e.ts`, the Henderson workspace | section 9: the household workspace (PR-2b) - the watermarked figure and the honest absent states | open the file; re-capture with `pnpm e2e` |
| `docs/evidence/pr2b-transcripts.tar` | `906d1134129dd297f9ffe600932f19e7fdd95c0abedb8ac01d32781371bcbd12` | 16896 | `tar -cf` over the PR-2b proof transcripts (one captured-evidence bundle per PR) | the naked-figure refusal at file:line, the watermark-suppression browser failure, and the restored green run (each with its superseded first attempt kept honest) | `tar -tf`, then `tar -xf` and read |
| `docs/evidence/pr2c-transcripts.tar` | `e1263642df49e42de1573c9227c13abac42be3f7e232d4da6496faf8c5e31b61` | 19456 | `tar -cf` over the PR-2c proof transcripts (one captured-evidence bundle per PR) | the GD-003 PII-sharpening companion battery, the full section 6 battery re-run with its RLS-dropped counter-run and restore, the session-lifecycle transcript, and the restored green run naming the keyboard-only walkthrough spec | `tar -tf`, then `tar -xf` and read |
