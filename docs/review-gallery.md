# Review gallery - every bucket-B artifact, indexed (CONSTITUTION.md, the review budget)

One row per binary or captured-evidence artifact: path, SHA-256, bytes, authorship, purpose, inspection.

| Path | SHA-256 | Bytes | Authorship / capture | Purpose | Inspect |
|---|---|---:|---|---|---|
| `src/app/fonts/GeistVF.woff2` | `46525b05bbf4527d263f8624ca7024c7d0dce3c64430267d30c31c6dd3821e4d` | 58512 | Vercel's Geist variable font, extracted from npm `geist@1.5.1` `package/dist/fonts/geist-sans/Geist-Variable.woff2` (`npm pack geist@1.5.1`) | self-hosted `next/font/local` typeface; `next/font/google` is forbidden (prompt 2 5A) | `shasum -a 256` and load any page |
| `docs/evidence/pr2a-signin.png` | `e90592e97527394bfebc528eb0dcd010d858c91cdebc51019214f15940d0e574` | 16248 | Playwright screenshot at 1280x800 from `src/e2e/app.e2e.ts` (URL and loaded-state marker asserted first) | section 9: the sign-in surface as shipped | open the file; re-capture with `pnpm e2e` |
| `docs/evidence/pr2a-shell.png` | `1fd9b21132cf0d0a8cea1ea1c15073c300002d3272147d52f80733f52c172454` | 33853 | Playwright screenshot at 1280x800 from `src/e2e/app.e2e.ts`, signed in as the seeded advisor | section 9: the honest empty shell PR-2a lands on (the split's user-visible result) | open the file; re-capture with `pnpm e2e` |
| `docs/evidence/pr2a-transcripts.tar` | `91bf94158d12bc862f2453341158436ef8abf7eec384ed5a7382e29e62915ad6` | 73216 | `tar -cf` over the PR-2a mutation and proof transcripts (one captured-evidence bundle per PR, prompt 2 section 7; plain tar so the release-artifact classifier never mistakes evidence for a release subject) | every raw transcript behind `docs/proof-log.md`'s PR-2a entries: M-B, M-D, M-E, M-F, M-G, the collector-sharpening proofs, pin currency, migration preflight, the restored green run, and the F2/F5 amendments from the independent pass; `m-e-7-8.txt` and `m-e-9-runtime.txt` also keep superseded first attempts as historical raw evidence - `m-e-8-fixed.txt` and `m-e-9-rerun.txt` are the operative records | `tar -tf`, then `tar -xf` and read |
| `docs/evidence/verin-0.2.0.tgz` | `6e246a048930507266f8a6c2b29a1fef1dff5a1f8ba1654c07dc154aeab60b0e` | 161097 | `corepack pnpm pack` at the PR head's source (a release artifact never contains its own checksum row, so its bytes predate this line) | the first real release subject: E15 leaves its honest-empty state against it, with `docs/evidence/verin-0.2.0.cdx.json` (bucket G, 291 components) equal to the frozen lockfile in both directions | `tar -tzf`; `corepack pnpm db sbom` regenerates the SBOM byte-identically |
| `docs/evidence/pr2a2-households.png` | `f1a020215ccad81fa5b751cb44e87559940c1782343ba4cdd92d59ee4207399a` | 30049 | Playwright screenshot at 1280x800 from `src/e2e/app.e2e.ts`, Firm A's advisor on the register | section 9: the household register (PR-2a' - the split's deferred screenshot), every seeded row wearing its demonstration chip | open the file; re-capture with `pnpm e2e` |
| `docs/evidence/pr2a2-transcripts.tar` | `de1b444ee0c183d45abaf485aaf8e16cddbf1397f8ff684039be87c58b8cd401` | 14848 | `tar -cf` over the PR-2a' proof transcripts (one captured-evidence bundle per PR) | the household-register smuggling refusal with its exact mutated bytes, the unregistered-step refusal, the RLS-removed counter-run failing and the restored pass, and the restored green run | `tar -tf`, then `tar -xf` and read |
| `docs/evidence/pr2b-workspace.png` | `f9a2c6f9bd50efbc6681aace5c02e3a95f834c42ceec75ec932c4ebcf45d28af` | 85072 | Playwright screenshot at 1280x800 from `src/e2e/app.e2e.ts`, the Henderson workspace | section 9: the household workspace (PR-2b) - the watermarked figure and the honest absent states | open the file; re-capture with `pnpm e2e` |
| `docs/evidence/pr2b-transcripts.tar` | `906d1134129dd297f9ffe600932f19e7fdd95c0abedb8ac01d32781371bcbd12` | 16896 | `tar -cf` over the PR-2b proof transcripts (one captured-evidence bundle per PR) | the naked-figure refusal at file:line, the watermark-suppression browser failure, and the restored green run (each with its superseded first attempt kept honest) | `tar -tf`, then `tar -xf` and read |
| `docs/evidence/pr2c-transcripts.tar` | `e1263642df49e42de1573c9227c13abac42be3f7e232d4da6496faf8c5e31b61` | 19456 | `tar -cf` over the PR-2c proof transcripts (one captured-evidence bundle per PR) | the GD-003 PII-sharpening companion battery, the full section 6 battery re-run with its RLS-dropped counter-run and restore, the session-lifecycle transcript, and the restored green run naming the keyboard-only walkthrough spec | `tar -tf`, then `tar -xf` and read |
| `docs/evidence/pr3a-evidence.png` | `e2643ce3de32c6d10f51e9140afc161799b1f89d05fe3da6399e3f1737c77f0d` | 107797 | Playwright screenshot at 1280x800 from `src/e2e/app.e2e.ts` (URL and loaded-state marker asserted first) | section 9: the evidence surface in its present state - every observation with source, both timestamps, its band and its demonstration chip | open the file; re-capture with `pnpm e2e` |
| `docs/evidence/pr3a-transcripts.tar` | `78e6a1f4e34ad13189c03c066b2ebd1ffb87c116a07d447c44fd6419d100db57` | 61952 | `tar -cf` over the PR-3a mutation and proof transcripts (one captured-evidence bundle per PR) | every raw transcript behind `docs/proof-log.md`'s PR-3a entries: the M-A/M-B/M-C/M-E batteries with exact injected bytes, the two-time-zone serialization proof, the migration and seed transcripts, the E16 slice evidence, the GD-004 companions, and both restored green runs (suite/checker and build/browser-with-axe) | `tar -tf`, then `tar -xf` and read |
| `docs/evidence/pr3b-delgado.png` | `f0c8b410fba079e22d18600f27cf568d2aaff014d890f41d620b1827c6f00deb` | 111312 | Playwright screenshot at 1280x800 from `src/e2e/app.e2e.ts` (URL and loaded-state marker asserted first); recaptured at PR-4a's head - the PR-3b falsification pass's Finding 2: the prior capture predated the merged E7 conflict-banner fix | section 9: the evidence surface receded - stale faded, aging banded, both sides of a conflict retained, one typed absence | open the file; re-capture with `pnpm e2e` |
| `docs/evidence/pr3b-okonkwo.png` | `f0a20d17e8693316088964ad6c26515068a9ec33e7fc8c12f805adf9aa79c9fd` | 100673 | Playwright screenshot at 1280x800 from `src/e2e/app.e2e.ts` (URL and loaded-state marker asserted first) | section 9: the vacuity state (M-D) - four typed-absence cards, each with its real next step | open the file; re-capture with `pnpm e2e` |
| `docs/evidence/pr3b-transcripts.tar` | `85bae983c7eea11d9b667a8e52824f94cbdf1a142dd2c7de552cdaea249f650e` | 20480 | `tar -cf` over the PR-3b mutation and proof transcripts (one captured-evidence bundle per PR) | every raw transcript behind `docs/proof-log.md`'s PR-3b entries: the M-D, conflict, freshness, point-in-time, truncation and axe companions with exact injected bytes, the E16 v2-effect evidence, and both restored green runs | `tar -tf`, then `tar -xf` and read |
| `docs/evidence/pr4a-policy.png` | `9139a2b33f4bd46ff81d8762368a165638f576244b0fe554d640a541679aa6d7` | 93448 | Playwright screenshot at 1280x800 from `src/e2e/app.e2e.ts` (loaded-state marker asserted first) | section 9: the policy shelf's inspect view - Firm A's version 1 resolved by content address, typed silence rendered as itself, the demonstration chip | open the file; re-capture with `pnpm e2e` |
| `docs/evidence/pr4a-transcripts.tar` | `ea0afe85c1cdd3022c9e27bc6dbcfa1a00f55d91a0e8aa7c29b9844641212d88` | 39936 | `tar -cf` over the PR-4a mutation and proof transcripts (one captured-evidence bundle per PR) | every raw transcript behind `docs/proof-log.md`'s PR-4a entries: the M-G arms with exact injected bytes and reverts, the GD-006 companions, the migration and seed transcript, the committed battery and the restored green run | `tar -tf`, then `tar -xf` and read |
| `docs/evidence/pr4b-policy.png` | `d228777bd42ff92209ab3c24310b6d72abf3cb240335ff31c0ea760dbdfe7d35` | 155448 | Playwright screenshot at 1280x800 from `src/e2e/app.e2e.ts` (loaded-state marker asserted first) | section 9: the policy shelf complete - the in-force line derived from the sequence, the history register, and the publish form | open the file; re-capture with `pnpm e2e` |
| `docs/evidence/pr4b-transcripts.tar` | `d10dc21075bbeee1f1187ea19ccc7fa7c4867961c1ceb7cc16cdcbebe8f9f1a5` | 20480 | `tar -cf` over the PR-4b mutation and proof transcripts (one captured-evidence bundle per PR) | every raw transcript behind `docs/proof-log.md`'s PR-4b entries: the M-G arm and companions with exact injected bytes and reverts, the amplification report with both regimes' diffs, the committed battery and the restored green run | `tar -tf`, then `tar -xf` and read |
| `docs/evidence/pr5a1-decide-blocked.png` | `575f9fa3583b83cbd5d347418cb6fd7d843e4814e2ed4c2400510676d0fd808b` | 93900 | Playwright screenshot at 1280x800 from `src/e2e/app.e2e.ts` (URL and loaded-state marker asserted first) | section 9: the decision surface's first honest LIVE state - blocked with the resolving step named, the DecisionId, and all three citations | open the file; re-capture with `pnpm e2e` |
| `docs/evidence/pr5a1-transcripts.tar` | `4f9da4072843e1a53084f49b00b11d740e99732042478237c5ef928059cb7b03` | 94720 | `tar -cf` over the PR-5a-i mutation and proof transcripts (one captured-evidence bundle per PR) | every raw transcript behind `docs/proof-log.md`'s PR-5a-i entries: the M-G closure and emission arms with exact injected bytes, the M-H compile refusals and construction result, the sharpened M-C stub kill, the two-time-zone determinism proof and the restored green run | `tar -tf`, then `tar -xf` and read |
| `docs/evidence/pr5a2-decide-proceed.png` | `8c6ef8cabb51e380c00b167e09e3567c613b45641bc0484df7768306efcfb5af` | 128802 | Playwright screenshot at 1280x800 from `src/e2e/app.e2e.ts` (URL and loaded-state marker asserted first) | section 9: the LIVE proceed - stages from the stated block, the CD-4d key, the CD-4e ordering line, figures through the watermarked metric, both registers | open the file; re-capture with `pnpm e2e` |
| `docs/evidence/pr5a2-decide-blocked.png` | `5863bf6634493c56fc4a792b0e1dce6cf7cd6e9f2056f58245952efa322471b3` | 106343 | Playwright screenshot at 1280x800 from `src/e2e/app.e2e.ts` | section 9: the LIVE breach - the arithmetic shown, the resolving step named, no key anywhere | open the file; re-capture with `pnpm e2e` |
| `docs/evidence/pr5a2-decide-prohibited.png` | `da5ced94d7f79fcc8d6ecf36a9e0bc0cbdef557388f45a8a9ac5324b28f22aa5` | 117369 | Playwright screenshot at 1280x800 from `src/e2e/app.e2e.ts` | section 9: the LIVE prohibition - the stamp, the exact regulatory version cited, zero affordances | open the file; re-capture with `pnpm e2e` |
| `docs/evidence/pr5a2-decide-silence.png` | `244d7e82a09d6efca1b81a7ad880fed870d5d0e02e68007f4c0b9ccfb203eee4` | 110665 | Playwright screenshot at 1280x800 from `src/e2e/app.e2e.ts` | section 9: the ratified typed-silence refusal, live - no stage invented where the contract states none | open the file; re-capture with `pnpm e2e` |
| `docs/evidence/pr5a2-transcripts.tar` | `2a90906925aea5ec8364b9f79f2473e6df4bfcb33ca6e85502d0d5779348b603` | 30720 | `tar -cf` over the PR-5a-ii mutation and proof transcripts (one captured-evidence bundle per PR) | every raw transcript behind `docs/proof-log.md`'s PR-5a-ii entries: the M-C arithmetic stub, the rule-removal counterexamples, the invented-approval typed-silence arm, the regenerated arm-06 missing-root refusal (the PR-5a-i falsification ruling's first obligation), and the restored green run with the two-time-zone byte-identity | `tar -tf`, then `tar -xf` and read |
| `docs/evidence/pr5a3-transcripts.tar` | `50af5c185c8cd5564ca9862f6645a4af5b7a229ee6a5439fb21255f1d2f7873c` | 41472 | `tar -cf` over the PR-5a-iii mutation and proof transcripts (one captured-evidence bundle per PR; every transcript carries its clean-tree control) | every raw transcript behind `docs/proof-log.md`'s PR-5a-iii entries: both M-A halves, closure containment, the extraction stub, the broken comparison mapping, the regenerated 20b/21b/22b discharging the carried PR-5a-ii obligation, and the restored green run | `tar -tf`, then `tar -xf` and read |
| `docs/evidence/pr5b-compare.png` | `00e7006bee2c90f38644964a09fe7c571fbc1e6aa232689a98688a9a78cf2dc3` | 129897 | Playwright screenshot at 1280x800 from `src/e2e/app.e2e.ts` (URL and loaded-state marker asserted first) | section 9: the comparison surface - one request, one bundle, one engine identity; Firm A proceeds with dual approval, Firm B refuses on the typed silence | open the file; re-capture with `pnpm e2e` |
| `docs/evidence/pr5b-transcripts.tar` | `ae033b3e3c7a713afbe15810f533255ba875bc741a6e0717210ac77ff4557b95` | 20992 | `tar -cf` over the PR-5b mutation and proof transcripts (one captured-evidence bundle per PR; every transcript carries its clean-tree control) | every raw transcript behind `docs/proof-log.md`'s PR-5b entries: the identity-drift arm with its regen jaw, manifest-drift, archetype-drift, shared-side-identity, and the restored green run | `tar -tf`, then `tar -xf` and read |
| `docs/evidence/pr5c1-transcripts.tar` | `1e9659a0b5639eec6a988573a6413cc5090fa040f02a5a6b6c09b9361d271a06` | 1495552 | `tar -cf` over the PR-5c-i mutation and proof transcripts plus the register screenshot (one captured-evidence bundle per PR; every mutation transcript carries its clean-tree control; pr5c1-conformance.png inside is the Playwright full-page capture from `src/e2e/app.e2e.ts`, URL and loaded-state marker asserted first) | every raw transcript behind `docs/proof-log.md`'s PR-5c-i entries - the M-B pre-parse hash refusal with both digests, both M-D ledger directions, both M-E vacuity halves, the M-F two-zone byte-identity, the restored green run - and the section-5C register: all sixteen signed cases, every binding field, three-valued with its ruling visible | `tar -tf`, then `tar -xf` and read; re-capture the screenshot with `pnpm e2e` |
